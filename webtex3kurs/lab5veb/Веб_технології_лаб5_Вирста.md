## Міністерство освіти і науки України

## Львівський національний університет природокористування

### Факультет механіки, енергетики та інформаційних технологій

#### Кафедра інформаційних технологій

---

# **Звіт**

про виконання лабораторної роботи №5  
з дисципліни **«Веб технології та веб дизайн»**  
на тему: **Створення веб-сервера з використанням Express.js**

**Виконав:** студент групи **КН-31** Вирста Андрій  
**Прийняв:** викл. **Тарас Квасниця**

**Львів — 2025**

---

## **Мета роботи**

- Ознайомитися з фреймворком Express.js.  
- Навчитися створювати маршрути та передавати HTML і JSON-інформацію.  
- Навчитися працювати зі статичними файлами та збереженням даних у файл.
- Розібратися з middleware, POST-запитами та структурою Express-проєкту.

---

## **Теоретичні відомості**

1. **Express.js**

Express.js — це гнучкий веб-фреймворк для Node.js, який спрощує створення REST API, веб-сайтів та серверної логіки. Він базується на роботі вбудованого модуля `http`, але надає зручніший спосіб обробки маршрутів, запитів і відповідей.

2. **Основні властивості Express:**

- зручні маршрути (`app.get()`, `app.post()` тощо);
- підтримка JSON-відповідей;
- робота зі статичними файлами;
- middleware — проміжні функції для обробки запиту;
- підтримка шаблонів та серверного рендерингу.

3. **Middleware**

Middleware — функції, які виконуються між отриманням запиту та формуванням відповіді. Наприклад, логування, автентифікація, парсинг JSON.

```js
app.use((req, res, next) => {
  console.log(`Запит: ${req.method} ${req.url}`);
  next();
});
```

4. **Статичні файли**

У Express статичні файли (HTML, CSS, JS, картинки) роздаються через:

```js
app.use(express.static('public'));
```

5. **POST-дані та body-parser**

Для отримання введених користувачем даних використовується middleware:

```js
app.use(bodyParser.urlencoded({ extended: true }));
```

---

## **Хід роботи (Практична частина)**

### **1. Ініціалізація проєкту**

У терміналі виконано:

```bash
npm init -y
npm install express body-parser
```

### **2. Структура проєкту**

```
lab5/
│
├── app.js
├── student.json (автоматичне створення)
└── public/
    ├── index.html
    ├── update.html
    └── style.css
```

### **3. Вміст файлу `app.js`**

```js
const express = require('express');
const fs = require('fs');
const bodyParser = require('body-parser');

const app = express();
const PORT = 3000;

app.use(express.static('public'));
app.use(bodyParser.urlencoded({ extended: true }));

const studentFile = 'student.json';

const defaultStudent = {
  name: "Андрій",
  group: "КН-31",
  message: "Я вчуся працювати з Express.js!"
};

if (!fs.existsSync(studentFile)) {
  fs.writeFileSync(studentFile, JSON.stringify(defaultStudent, null, 2), 'utf-8');
  console.log("Файл student.json створено.");
}

app.get('/', (req, res) => {
  res.sendFile(__dirname + '/public/index.html');
});

app.get('/student', (req, res) => {
  const data = JSON.parse(fs.readFileSync(studentFile, 'utf-8'));
  res.send(`
    <h1>Інформація про студента</h1>
    <p><strong>Ім'я:</strong> ${data.name}</p>
    <p><strong>Група:</strong> ${data.group}</p>
    <p><strong>Повідомлення:</strong> ${data.message}</p>
    <a href="/update">Редагувати</a>
  `);
});

app.get('/json', (req, res) => {
  const data = JSON.parse(fs.readFileSync(studentFile, 'utf-8'));
  res.json(data);
});

app.get('/update', (req, res) => {
  res.sendFile(__dirname + '/public/update.html');
});

app.post('/update', (req, res) => {
  const updatedData = {
    name: req.body.name,
    group: req.body.group,
    message: req.body.message
  };

  fs.writeFileSync(studentFile, JSON.stringify(updatedData, null, 2), 'utf-8');
  res.redirect('/student');
});

app.listen(PORT, () => {
  console.log(`Сервер працює: http://localhost:${PORT}`);
});
```

---

### **4. Вміст HTML-файлів**

📌 `public/index.html`

```html
<h1>Ласкаво просимо!</h1>
<p>Сервер працює на Express.js</p>
```

📌 `public/update.html`

```html
<form action="/update" method="POST">
  <input type="text" name="name" placeholder="Ім'я" required>
  <input type="text" name="group" placeholder="Група" required>
  <textarea name="message" required></textarea>
  <button type="submit">Зберегти</button>
</form>
```

---

## **Результат роботи**

- Сервер успішно створено з використанням Express.js.
- Дані виводяться у форматах HTML і JSON.
- Реалізовано редагування інформації через форму.
- Дані зберігаються у `student.json`.

---

## **Висновок**

Під час виконання лабораторної роботи я навчився:

- створювати сервер на Express.js;
- працювати з маршрутами GET та POST;
- використовувати middleware та body-parser;
- працювати зі статичними файлами;
- зберігати і оновлювати дані у форматі JSON.

Ця робота є важливим етапом у переході від простих серверів Node.js до повноцінних веб-додатків та API.

---

## **Контрольні запитання**

**1. Що таке Express.js і для чого він використовується?**  
Фреймворк Node.js для створення веб-серверів і API.

**2. У чому перевага Express над http-модулем?**  
Підтримка роутінгу, middleware і менше коду.

**3. Що таке маршрут (route)?**  
Шлях URL, на який реагує сервер.

**4. Які є типи HTTP-методів?**  
GET, POST, PUT, DELETE.

**5. Як додати middleware у Express?**  
Через `app.use()`.

**6. Як відправити JSON відповідь?**  
Через `res.json()`.

**7. Як зробити сервер доступним для статичних файлів?**  
`app.use(express.static('public'));`

---

